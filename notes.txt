=> serverless login
=> Export the credentials directly to serverless.com [IAM]

# Clear local serverless configuration caches

=>rm -rf ~/.serverless ~/.serverlessrc

# Inject fresh AWS IAM static authorization profiles

=>  export AWS_ACCESS_KEY_ID="Your_Access_Key_Id"
=>  export AWS_SECRET_ACCESS_KEY="Your_Secret_Access_Key"
=>  export AWS_DEFAULT_REGION="select_region"


#Verify Infrastructure Key Integrity

=> aws sts get-caller-identity

#To deplpoy

=>serverless deploy

# wipe out all live infrastructure objects in the AWS cloud
while keeping your local code repository

=>serverless remove